import React from 'react';

import Styles from './MessageButton.module.scss';

// TODO: move to module, rename to Chat

const MessageButton = () => (
  <div className={Styles.message_button}>
    <button type="button" />
  </div>
);

export default MessageButton;
